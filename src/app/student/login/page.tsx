import StudentLoginPageClient from './StudentLoginPageClient';

export const metadata = {
  title: 'Student Portal - CR Attendance Manager',
  description: 'Login to your student attendance portal.',
};

export default function StudentLoginPage() {
  return <StudentLoginPageClient />;
}
